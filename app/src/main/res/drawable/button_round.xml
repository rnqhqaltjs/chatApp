<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 전체적인 배경색 -->
    <solid android:color="@color/lsb"/>

    <!-- 테두리 색상과 두께 -->
    <stroke
        android:width="1dp"
        android:color="#000000"/>

    <!-- 모서리의 반지름 -->
    <corners
        android:radius="30dp"/>

    <stroke
        android:width="1dp"
        android:color="@color/black" />

</shape>